# Part C - Transaction Management with Spring + Hibernate (Bank Transfer)
Demonstrates @Transactional transfer between accounts using Spring and Hibernate.
Configure DB in src/main/resources/hibernate.cfg.xml then:
  mvn compile exec:java -Dexec.mainClass="com.example.bank.BankApp"
