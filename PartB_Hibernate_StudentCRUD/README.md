# Part B - Hibernate Student CRUD
Console application showing basic CRUD operations for Student entity using Hibernate + MySQL.
Edit src/main/resources/hibernate.cfg.xml with your DB credentials, then:
  mvn compile exec:java -Dexec.mainClass="com.example.hibernate.StudentApp"
SQL script to create schema: sql/create_student_db.sql
